"use strict"; // Treat all JS code as newer version

console.log(8 + 9);

let name = "Saurav verma";
let age = 25;
let isLoggedIn = true;

// number - all positive, negative integers , 0 and all decimal and fraction numbers.
// bigInt - a number very large
//  string -
//  boolean - true or false
// null - standalone value, intentional representation of empty value.
//  undefined - value is not defined
// symbol - unique value
